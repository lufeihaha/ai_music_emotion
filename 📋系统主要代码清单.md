# 🎵 音乐情感识别系统 - 主要代码清单

## 📊 代码规模统计

- **总代码文件**: 50+ Python模块
- **总代码行数**: 约20,000行
- **核心功能模块**: 12个主要模块
- **Web应用模块**: 4个版本
- **数据处理模块**: 8个模块
- **分析工具模块**: 6个模块

---

## 🌐 Web应用层代码

### 1. **主要Web应用** - `src/web_app.py` (824行)
```python
class MusicEmotionPredictor:
    def __init__(self):
        """智能模型选择机制"""
        # 按优先级加载模型：
        # 1. 优化增强模型 (80%+准确率)
        # 2. 增强情感模型 (77%准确率)  
        # 3. 改进针对性模型 (67.5%准确率)
        # 4. 综合8种情感模型 (46.3%准确率)
        
    def extract_features(self, audio_path):
        """提取完整音频特征"""
        # MFCC特征 + 频谱特征 + 色度特征
        # 时域特征 + 节奏特征 + 深度学习特征
        
    def predict_emotion(self, audio_path):
        """智能情感预测"""
        # 特征提取 → 特征选择 → 模型预测 → 置信度校准

@app.route('/predict', methods=['POST'])
def predict():
    """单文件情感预测API"""
    
@app.route('/batch_predict', methods=['POST']) 
def batch_predict():
    """批量文件处理API"""
    
@app.route('/feedback', methods=['POST'])
def collect_feedback():
    """用户反馈收集API"""
```

### 2. **多模态Web应用** - `src/multimodal_web_app.py` (640行)
```python
class MultiModalMusicEmotionPredictor:
    """音频+歌词的多模态融合分析"""
    
    def predict_multimodal(self, audio_path: str, lyrics: str):
        """多模态情感预测（音频+歌词）"""
        audio_result = self.predict_audio_only(audio_path)
        lyrics_result = self.predict_lyrics_only(lyrics)
        fused_result = self.fusion_engine.fuse_emotions(audio_result, lyrics_result)
        
    def predict_adaptive(self, audio_path: str, lyrics: Optional[str] = None):
        """自适应预测（根据可用信息选择最佳方法）"""
```

### 3. **中文优化Web应用** - `src/chinese_web_app.py` (290行)
```python
"""专门针对中文音乐优化的Web应用"""
# 8种中文情感分类：快乐、平静、激昂、忧郁、怀念、浪漫、深沉、激烈
```

---

## 🤖 AI模型层代码

### 1. **核心情感分类器** - `src/emotion_classifier.py` (329行)
```python
class EmotionNet(nn.Module):
    """深度学习情感识别网络"""
    def __init__(self, input_size, num_emotions):
        super(EmotionNet, self).__init__()
        self.model = nn.Sequential(
            nn.Linear(input_size, 128),
            nn.ReLU(),
            nn.Dropout(0.3),
            nn.Linear(128, 64),
            nn.ReLU(),
            nn.Dropout(0.3),
            nn.Linear(64, 32),
            nn.ReLU(),
            nn.Linear(32, num_emotions),
            nn.Softmax(dim=1)
        )

class TraditionalClassifier:
    """传统机器学习分类器"""
    SUPPORTED_MODELS = {
        'svm': SVC,
        'random_forest': RandomForestClassifier
    }
    
    def _perform_grid_search(self, X, y):
        """网格搜索超参数优化"""
        param_grid = self._get_param_grid()
        grid_search = GridSearchCV(self.model, param_grid, cv=5, n_jobs=-1)
```

### 2. **中文情感分类器** - `src/chinese_emotion_classifier.py` (462行)
```python
class ChineseEmotionClassifier:
    """纯中文音乐情感分类器"""
    
    def __init__(self):
        # 定义8种中文情感分类体系
        self.chinese_emotions = {
            '快乐': {'icon': '😊', 'color': '#FFD700', 'keywords': ['开心', '愉快']},
            '平静': {'icon': '🍃', 'color': '#90EE90', 'keywords': ['安静', '宁静']},
            '激昂': {'icon': '⚡', 'color': '#FF6347', 'keywords': ['激动', '热情']},
            '忧郁': {'icon': '☁️', 'color': '#9370DB', 'keywords': ['悲伤', '忧愁']},
            '怀念': {'icon': '⏰', 'color': '#CD853F', 'keywords': ['思念', '回忆']},
            '浪漫': {'icon': '💕', 'color': '#FF69B4', 'keywords': ['甜蜜', '温柔']},
            '深沉': {'icon': '🌙', 'color': '#4B0082', 'keywords': ['深刻', '沉重']},
            '激烈': {'icon': '🎭', 'color': '#DC143C', 'keywords': ['强烈', '紧张']}
        }
    
    def create_chinese_training_data(self):
        """基于中文音乐特征生成训练数据"""
        emotion_templates = {
            '快乐': {'tempo': 120, 'key_mode': 0.8, 'emotional_intensity': 0.8},
            '平静': {'tempo': 70, 'key_mode': 0.6, 'emotional_intensity': 0.4},
            # ... 其他情感模板
        }
```

### 3. **集成学习模型** - `src/ensemble_learning.py` (448行)
```python
class AdvancedEnsembleClassifier:
    """高级集成学习分类器"""
    
    def __init__(self):
        self.base_models = {
            'random_forest': RandomForestClassifier(n_estimators=200),
            'svm': SVC(probability=True, kernel='rbf'),
            'gradient_boosting': GradientBoostingClassifier(n_estimators=200),
            'logistic_regression': LogisticRegression(max_iter=1000),
            'mlp': MLPClassifier(hidden_layer_sizes=(128, 64), max_iter=1000)
        }
        
    def create_meta_model(self):
        """创建元学习器"""
        self.meta_model = LogisticRegression()
        
    def stacking_predict(self, X):
        """堆叠集成预测"""
        # 第一层：基础模型预测
        base_predictions = self._get_base_predictions(X)
        # 第二层：元模型融合
        final_prediction = self.meta_model.predict(base_predictions)
```

---

## 🔧 特征工程层代码

### 1. **特征提取器** - `src/feature_extraction.py` (256行)
```python
def extract_features(audio_path):
    """从音频文件中提取完整特征集"""
    
    # 1. MFCC特征 (音色)
    mfcc = librosa.feature.mfcc(y=y, sr=sr, n_mfcc=20)
    mfcc_mean = np.mean(mfcc, axis=1)  # (20,)
    mfcc_std = np.std(mfcc, axis=1)    # (20,)
    
    # 2. 频谱特征 (频率分布)
    spectral_centroids = librosa.feature.spectral_centroid(y=y, sr=sr)[0]
    spectral_bandwidth = librosa.feature.spectral_bandwidth(y=y, sr=sr)[0]
    spectral_rolloff = librosa.feature.spectral_rolloff(y=y, sr=sr)[0]
    spectral_contrast = librosa.feature.spectral_contrast(y=y, sr=sr)
    
    # 3. 色度特征 (和声)
    chroma = librosa.feature.chroma_stft(y=y, sr=sr)
    chroma_cqt = librosa.feature.chroma_cqt(y=y, sr=sr)
    
    # 4. 时域特征
    zero_crossing_rate = librosa.feature.zero_crossing_rate(y)[0]
    rms = librosa.feature.rms(y=y)[0]
    
    # 5. 节奏特征
    tempo, beats = librosa.beat.beat_track(y=y, sr=sr)
    
    # 6. 深度学习特征
    mel_spec = librosa.feature.melspectrogram(y=y, sr=sr, n_mels=128)
    C = np.abs(librosa.cqt(y=y, sr=sr))
    
    # 合并所有特征 → 4864维特征向量
    features = np.concatenate([...])
```

### 2. **音频处理器** - `src/audio_processor.py` (281行)
```python
class AudioProcessor:
    """音频文件处理和预处理"""
    
    def process_audio_file(self, file_path):
        """处理单个音频文件"""
        
    def normalize_audio(self, audio_data):
        """音频标准化"""
        
    def segment_audio(self, audio_data, segment_length=30):
        """音频分段处理"""
        
    def extract_rhythm_features(self, audio_data, sr):
        """提取节奏特征"""
```

### 3. **多模态预测器** - `src/multimodal_predictor.py` (274行)
```python
class MultiModalMusicEmotionPredictor:
    """多模态音乐情感预测器"""
    
    def predict_multimodal(self, audio_path: str, lyrics: str):
        """多模态情感预测（音频+歌词）"""
        # 音频分析
        audio_result = self.predict_audio_only(audio_path)
        # 歌词分析 
        lyrics_result = self.predict_lyrics_only(lyrics)
        # 融合结果
        fused_result = self.fusion_engine.fuse_emotions(audio_result, lyrics_result)
        
    def predict_adaptive(self, audio_path: str, lyrics: Optional[str] = None):
        """自适应预测（根据可用信息选择最佳预测方法）"""
```

---

## 📊 数据处理层代码

### 1. **数据增强器** - `src/data_augmentation.py` (186行)
```python
class AudioDataAugmentation:
    """音频数据增强"""
    
    def add_noise(self, audio, noise_factor=0.005):
        """添加随机噪声"""
        
    def time_stretch(self, audio, rate=1.0):
        """时间拉伸/压缩"""
        
    def pitch_shift(self, audio, sr, n_steps=0):
        """音调变换"""
        
    def augment_dataset(self, input_dir, output_dir):
        """批量数据增强"""
        # 每个原始文件生成3个增强版本：
        # 1. 噪声版本 (noise_factor=0.005, 0.010)
        # 2. 音调版本 (pitch_shift=-1, +1)
        # 3. 速度版本 (time_stretch=0.9, 1.1)
```

### 2. **中文数据集成器** - `src/chinese_data_integration.py` (475行)
```python
class ChineseDataIntegrator:
    """中文音乐数据集成器"""
    
    def integrate_datasets(self):
        """整合多个中文音乐数据集"""
        # GTZAN数据集 + PMEmo数据集 + 自建中文数据集
        
    def map_emotions_to_chinese(self, emotion_mapping):
        """将情感标签映射为中文"""
        emotion_mapping = {
            'happy': '快乐', 'sad': '忧郁', 'calm': '平静',
            'energetic': '激昂', 'romantic': '浪漫'
        }
```

---

## 📈 分析工具层代码

### 1. **用户反馈分析器** - `src/feedback_analyzer.py` (384行)
```python
class FeedbackAnalyzer:
    """用户反馈数据分析器"""
    
    def analyze_prediction_accuracy(self):
        """分析预测准确性"""
        # 整体准确率计算
        # 各情感类别准确率分析
        # 常见错误模式识别
        
    def analyze_confidence_issues(self):
        """分析置信度问题"""
        # 置信度分布分析
        # 置信度与准确性关系
        # 低置信度/高置信度错误案例
        
    def identify_problem_patterns(self):
        """识别问题模式"""
        # 系统性偏向检测
        # 特定情感识别困难
        # 低置信度问题识别
        
    def generate_improvement_suggestions(self):
        """生成改进建议"""
        # 基于分析结果的具体改进建议
```

### 2. **模型优化器** - `src/model_optimizer.py` (437行)
```python
class ModelOptimizer:
    """模型性能优化器"""
    
    def optimize_hyperparameters(self):
        """超参数优化"""
        
    def analyze_user_feedback(self):
        """分析用户反馈"""
        
    def retrain_with_feedback(self):
        """基于反馈重新训练"""
        
    def ensemble_optimization(self):
        """集成模型优化"""
```

### 3. **时序情感分析器** - `src/temporal_emotion_analyzer.py` (591行)
```python
class TemporalEmotionAnalyzer:
    """时序情感分析器"""
    
    def analyze_emotion_over_time(self, audio_path):
        """分析音频中情感随时间的变化"""
        
    def segment_analysis(self, audio_segments):
        """分段情感分析"""
        
    def create_emotion_timeline(self, results):
        """创建情感时间线"""
        
    def detect_emotion_transitions(self, timeline):
        """检测情感转换点"""
```

---

## 🎯 训练管理层代码

### 1. **综合情感训练器** - `src/comprehensive_emotion_trainer.py` (476行)
```python
class ComprehensiveEmotionTrainer:
    """综合情感训练器"""
    
    def train_8emotions_model(self):
        """训练8种情感分类模型"""
        
    def cross_validation_training(self):
        """交叉验证训练"""
        
    def ensemble_training(self):
        """集成模型训练"""
        
    def save_training_results(self):
        """保存训练结果和评估报告"""
```

### 2. **针对性改进器** - `src/targeted_model_improvement.py` (433行)
```python
class TargetedModelImprovement:
    """针对性模型改进"""
    
    def improve_based_on_feedback(self):
        """基于用户反馈改进模型"""
        
    def balance_emotion_data(self):
        """平衡情感数据分布"""
        
    def enhance_feature_engineering(self):
        """增强特征工程"""
```

---

## 🛠️ 工具和配置代码

### 1. **中文字体配置** - `src/matplotlib_chinese_config.py` (75行)
```python
def setup_chinese_matplotlib():
    """配置matplotlib支持中文显示"""
    plt.rcParams['font.sans-serif'] = ['SimHei', 'Microsoft YaHei']
    plt.rcParams['axes.unicode_minus'] = False
```

### 2. **可视化工具** - `src/visualization.py` (约8KB)
```python
"""图表生成和可视化工具"""
def create_confusion_matrix()
def plot_feature_importance()
def generate_emotion_distribution()
def create_performance_charts()
```

---

## 📋 启动脚本

### Windows启动脚本
```batch
# start_app.bat
@echo off
echo 🎵 启动音乐情感识别系统...
python run_app.py
```

### Python启动脚本
```python
# run_app.py - 主启动脚本
# start_chinese_app.py - 中文优化版
# launch_apple_style.py - Apple风格版
# debug_app.py - 调试版本
```

---

## 🏆 代码质量特点

### 1. **代码规范**
- ✅ **完整注释**: 中英文注释，代码可读性强
- ✅ **模块化设计**: 功能解耦，易于维护
- ✅ **错误处理**: 完善的异常处理机制
- ✅ **日志系统**: 详细的运行日志记录

### 2. **技术特色**
- 🎯 **智能模型选择**: 自动选择最优可用模型
- 🔄 **用户反馈闭环**: 完整的反馈收集和分析
- 🌐 **多版本支持**: 标准版、中文版、多模态版
- 📊 **完整监控**: 性能监控和问题诊断

### 3. **工程实践**
- 🏗️ **分层架构**: Web层、AI层、数据层清晰分离
- 🔧 **配置管理**: 集中化配置和参数管理
- 📈 **可扩展性**: 易于添加新模型和新功能
- 🛠️ **部署友好**: 多种启动方式和环境适配

---

**总结**: 这是一个**技术栈完整、功能丰富、工程质量高**的音乐情感识别系统，代码涵盖了从数据处理到Web应用的完整技术链路，具有很高的学术价值和实用价值。 